## REACT

### Exercise 1 - REACT with props & state
Internal javascript, objects manipulation, promt function and React props & state.

![Ex 1 website]()

Create a React component `ImageComponent`, it has to
- Show an image and its title

Create a React component `Gallery`, it has to
- Initialize its status with the data array
- Create an `ImageComponent` for each data
- Use a flexbox horizontal design

Add a button to add a new immage (use `promt()` function)

### Exercise 2 - Registration Form with React
Internal javascript, external css, form inputs, POST request with React and React state.

![Ex 2 website]()

Implement the function:
- `handleChange` to update the state variables value each time the value of an input field changes
- `handleSubmit` to send the data, followed by a success or an error message

Note: use [httpbin.org](https://httpbin.org/post) for the POST.

### Exercise 3 - Products table with React
Internal javascript, internal css, table content and React state.

![Ex 3 website]()

Make a React GET request to show the products

Note: use [fakestoreapi.com](https://fakestoreapi.com/products) for the React GET.
