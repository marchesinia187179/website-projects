## FLASK and REACT

### Exercise 1 - Single page application
Internal javascript, external css, client-side routing in python and React components.

![Ex 1 website]()

From a starting website
- Convert the pages into React components
- Set up client-side routing to navigate between components

### Exercise 2 - Products table with Flask, React and csv
Internal javascript, external css, client-side routing in python and csv file.

![Ex 2 website]()

Implement `ProductsDetail` to show each information of a component (use a route like `/products/:productCode`) when its table row is clicked.
